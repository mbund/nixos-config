import common.caddyfile

# gitlab.mbund.org
gitlab.mbund.org:4430 www.gitlab.mbund.org:4430 {
    # www -> apex
    @www host www.gitlab.mbund.org
    redir @www https://gitlab.mbund.org{uri} permanent
    
    header {
        import set_headers
    }
}
